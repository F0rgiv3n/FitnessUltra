<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Units -->
    <PreferenceCategory android:title="Units">

        <ListPreference
            android:key="pref_distance_unit"
            android:title="Distance unit"
            android:summary="%s"
            android:entries="@array/pref_distance_entries"
            android:entryValues="@array/pref_distance_values"
            android:defaultValue="km"
            app:useSimpleSummaryProvider="false" />

        <ListPreference
            android:key="pref_weight_unit"
            android:title="Weight unit"
            android:summary="%s"
            android:entries="@array/pref_weight_entries"
            android:entryValues="@array/pref_weight_values"
            android:defaultValue="kg"
            app:useSimpleSummaryProvider="false" />

    </PreferenceCategory>

    <!-- Voice Feedback -->
    <PreferenceCategory android:title="Voice Feedback">

        <SwitchPreferenceCompat
            android:key="pref_voice_enabled"
            android:title="Enable voice announcements"
            android:summary="Announce distance milestones during a run"
            android:defaultValue="true" />

        <ListPreference
            android:key="pref_voice_frequency"
            android:title="Announce every"
            android:summary="%s"
            android:entries="@array/pref_voice_freq_entries"
            android:entryValues="@array/pref_voice_freq_values"
            android:defaultValue="1"
            android:dependency="pref_voice_enabled"
            app:useSimpleSummaryProvider="false" />

    </PreferenceCategory>

    <!-- Appearance -->
    <PreferenceCategory android:title="Appearance">

        <ListPreference
            android:key="pref_theme"
            android:title="Theme"
            android:summary="%s"
            android:entries="@array/pref_theme_entries"
            android:entryValues="@array/pref_theme_values"
            android:defaultValue="system"
            app:useSimpleSummaryProvider="false" />

    </PreferenceCategory>

    <!-- About -->
    <PreferenceCategory android:title="About">

        <Preference
            android:key="pref_version"
            android:title="Version"
            android:selectable="false" />

    </PreferenceCategory>

</PreferenceScreen>
